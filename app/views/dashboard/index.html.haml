dashboard
%br
name:
=current_user.full_name
%br
email:
= current_user.email
%br
memo:
= current_user.memo
%h1
  Write your memo!
  = form_for(current_user, url: dashboard_path, method: :put) do |form|
    = form.text_field :memo
    = form.submit "Send"
